---
name: {{.SampleSkill}}
description: A placeholder skill. Replace the description and instructions with your own before shipping.
---

# {{.SampleSkillTitleCase}}

## When to use

Claude Code loads this skill when the plugin is active. Narrow the trigger by adding `when-to-use` guidance that names a specific situation (file type, repo shape, user request).

## Instructions

Replace this section with concrete, imperative steps. Example:

1. Read the target file with `Read`.
2. Grep for the pattern `X` with `Grep`.
3. Summarise findings and propose the fix.

## Example

Show a before/after or a short transcript demonstrating the correct behaviour. Optional, but strongly recommended for skills that depend on context to trigger well.
